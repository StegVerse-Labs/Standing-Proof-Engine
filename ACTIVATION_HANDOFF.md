# Standing-Proof-Engine Activation Handoff

Date: 2026-06-28
Repo: `StegVerse-Labs/Standing-Proof-Engine`
Active branch: `main`
Status: activation handoff complete

## Completion statement

The stale recovery branch/PR path has been closed and manually cleaned up. `main` is the only active build line for this repository. No open PR is required for this handoff.

This handoff closes the current build task by declaring the repository ready for activation verification and downstream integration planning.

## Current verification surface

The primary workflow is `.github/workflows/verify.yml`.

It is configured to run on:

- `push`
- `pull_request`
- `workflow_dispatch`

The workflow verifies:

- pressure receipt samples
- stale-state review/commit samples
- Aegis incident standing samples
- manifest and commitment-candidate manifest samples
- SDK intake and SDK commitment-candidate intake receipts
- pointer and confirmation samples
- destination-event and event-replay samples, including deferred cases
- source-bound and external-reference samples
- frozen hash manifests and derived frozen hash bindings
- heartbeat path-selection receipts
- expected-result fixture coverage
- automated repo standing route and JSON export
- expected-corpus inventory diagnostics
- expected-corpus verifier JSON output
- reviewer reports and machine-readable handoff artifacts
- release-readiness reports
- formalism and handoff unit tests

## Required activation verification command set

A local or CI activation verification run should execute the workflow-equivalent command set:

```bash
python -m tools.refresh_frozen_hashes --check
python -m spe.verify_expected_corpus --json
python tools/run_repo_standing.py
python tools/run_repo_standing.py --json
python tools/run_repo_standing.py --write-reports
python tools/write_release_readiness.py
python -m unittest discover -s tests -p 'test_*.py'
```

The workflow also produces expected-corpus inventory diagnostics:

```bash
mkdir -p reports
python tools/expected_corpus_inventory.py > reports/expected_corpus_inventory.json
python tools/write_expected_corpus_failed_summary.py \
  reports/expected_corpus_inventory.json \
  reports/expected_corpus_failed_fixtures.json \
  reports/expected_corpus_failed_fixtures.md
python tools/verify_artifact_sha256.py \
  reports/expected_corpus_inventory.json \
  7d3daf3832ff27521aab38f87368c8505cdf203098b580cea62358add0be5756 \
  > reports/expected_corpus_inventory_sha256.json
```

## Expected activation artifacts

The activation run should produce and/or upload:

- `reports/expected_corpus_inventory.json`
- `reports/expected_corpus_failed_fixtures.json`
- `reports/expected_corpus_failed_fixtures.md`
- `reports/expected_corpus_inventory_sha256.json`
- reviewer report Markdown files under `reports/`
- machine-readable report JSON files under `reports/`
- release-readiness report files generated by `tools/write_release_readiness.py`

## Standing boundaries

This handoff does not claim external certification, mathematical proof of unrelated claims, or broad ecosystem completion.

It claims only that the current SPE repository build task has reached a clean activation handoff state:

- stale recovery PR path closed
- recovery branches manually deleted by the maintainer
- mainline build path active
- verification workflow documented
- expected artifacts documented
- downstream integration candidate identified

## Downstream integration candidate

The next integration target is the SDK / manifest-receipt path.

Reason: SPE should evaluate Commitment Candidate / Execution Authority Request material at the commit-time boundary. The SDK is the natural upstream producer of manifest/receipt-bound inputs that prevent ambiguous, informal, or authority-carrying material from entering the SPE path.

## Activation decision

Current task: complete.

Next task: verify CI/workflow artifact output from `main`, then begin SDK-to-SPE integration for commitment-candidate intake and commit-time standing determination.
